// Все числа игры в одном месте — прямой аналог GameTuning.cs из Unity-версии.
//
// Имена полей совпадают с Unity намеренно: веб здесь работает лабораторией.
// Когда ощущения сойдутся, значения переносятся обратно в GameTuning
// подстановкой, а не пересочиняются заново.
//
// Всё, что видно в панели настроек, читается контроллерами каждый кадр,
// поэтому ползунок меняет поведение сразу, без перезапуска.

export const tuning = {
  // --- Арена ---
  arenaRadius: 7.5,
  arenaThickness: 0.9,
  killY: -14,

  // --- Физика мира ---
  // Тяжелее реальной: падение с арены должно читаться быстро.
  gravity: -26,

  // --- Движение ---
  // Скорость выведена из геометрии ног, а не выбрана на глаз.
  //
  // Стопа отходит от таза не дальше сорока сантиметров: бедро на высоте
  // 0.81, длина ноги 0.82. Значит опора живёт от +0.30 впереди до −0.26
  // позади, тело продвигается на 0.34 м за шаг — ровно человеческие
  // пропорции, у человека это 0.37. При четырёх метрах в секунду ноги
  // просто не успевали переставляться: опора улетала за предел
  // досягаемости, IK срезала цель на 38 см, и разницу боец добирал
  // волочением стоп. Здесь — быстрый шаг, около четырёх шагов в секунду.
  maxRunSpeed: 1.6,
  // Доля скорости при ходе вбок и назад. Не условность жанра, а следствие
  // устройства ног: приставным шагом стопа уходит вбок недалеко — наружу
  // мешает досягаемость, внутрь вторая нога. На полной скорости вбок обе
  // ноги хотят оторваться разом, одна ждёт очереди, и её опору утаскивает
  // на 97 см при досягаемости в сорок.
  strafeSpeed: 0.5,
  backSpeed: 0.55,
  moveAccel: 16,
  moveBrake: 10,
  // Управление в воздухе малое намеренно: сбитый должен долетать до края,
  // а не выруливать обратно на арену.
  airControl: 4,
  // Разворот с разгоном. Плоская скорость читалась как мгновенный доворот
  // детали; теперь у поворота есть угловое ускорение, а перед целью он
  // тормозит ровно настолько, чтобы успеть остановиться.
  turnSpeed: 420,
  turnAccel: 1600,
  // Насколько теряется управление во время проноса. Удар должен чего-то стоить.
  swingMoveLock: 0.2,

  // --- Итерация физики тела ---
  // 2 — активный ragdoll на мышцах: поза это цель, тело её догоняет.
  // 3 — жёсткие панели на верёвках: корпус держится крепко, а локти и кисти
  //     почти отпущены и болтаются на своих связях. Связи при этом
  //     нерастяжимые: панель картонная, гнутся только суставы.
  // Первая итерация — кинематическое тело, где поза писала кости напрямую, —
  // живёт в истории, коммит 401d802.
  bodyMode: 3,
  // Насколько голова держится отвесно, не заваливаясь вслед за шеей.
  headLevel: 0.8,
  // Ближе этого руки не подходят к оси корпуса. У третьей итерации локоть
  // и кисть почти отпущены, и без запрета они качаются прямо в торс.
  torsoClearance: 0.16,
  // Зазор между панелями с каждого конца кости. Части тела висят не впритык:
  // на сгибе они не въезжают друг в друга, а в просвете видно нить, на которой
  // всё держится. Читается на сборке бойца — меняется после перезапуска.
  partGap: 0.018,

  // --- Оружие ---
  // Сейчас выключено: походку и желейность тела настраиваем на чистом теле.
  // Девять килограммов на правой кисти перекашивают ровно то, что мы
  // пытаемся оценить. Возвращается одним переключателем — ни поза,
  // ни скелет от этого не пересобираются.
  withClub: false,

  // --- Стойка ---
  // Руки висят вдоль тела почти прямыми, как в референсе, а не подняты
  // в боевую стойку. Кисть вынесена на 0.68 от плеча при длине руки 0.74 —
  // девять десятых размаха, так что локоть едва намечен. Ставить кисть
  // ровно на предел нельзя: IK упирается в границу досягаемости, и рука
  // начинает щёлкать между двумя решениями от любой мелочи.
  guardHeight: 0.66,
  guardForward: 0.05,
  // Кисти вынесены за силуэт головы. Голова на выкройке шире торса — пять
  // клеток против трёх, — и при узкой стойке она попросту закрывает руки
  // целиком, если смотреть спереди.
  guardWidth: 0.35,
  // Размах рук при ходьбе, в долях длины шага.
  armSwing: 0.55,
  stanceWidth: 0.09,
  // Подсед почти убран: колени в стойке прямые. За это платим длиной шага,
  // и связь тут жёсткая арифметическая. Бедро на 0.64 минус подсед, стопа
  // на 0.10, нога 0.58 — значит по горизонтали стопа отходит на
  // sqrt(0.58² − высота²), и из этого вычитается разнос ног вбок.
  // При подседе 0.06 выходило 0.33 м, при 0.025 только 0.25.
  // Отсюда же и предел суммы stepReach + stepTrigger: заступишь за него —
  // и опора окажется недостижимой, IK срежет цель, стопа поедет юзом.
  // Скорость добираем частотой шага, а не длиной: короткие ноги семенят.
  stanceCrouch: 0.02,

  // --- Шаг ---
  // Стопа втыкается в мировую точку и стоит, пока тело идёт над ней.
  // Отрывается, когда опора уехала от положенного места дальше порога —
  // порогом, а не расписанием, потому что одно правило закрывает разом
  // ходьбу, разворот на месте, остановку и восстановление после толчка.
  //
  // Порог обязан быть меньше, чем нога способна дотянуться: бедро на высоте
  // 0.81, длина ноги 0.82, значит по горизонтали она достаёт примерно
  // на 0.41. Выше этого нога распрямляется в струну и шаг запаздывает.
  stepTrigger: 0.16,
  // Длительность переноса на среднем ходу, секунд.
  stepTime: 0.18,
  // Насколько далеко вперёд забрасывать стопу, в долях того пути, который
  // тело пройдёт за время переноса. Ноль — шаг под себя, и боец отстаёт
  // от собственного хода.
  stepLead: 0.9,
  // Предел выноса опоры вперёд-назад. Больше этого нога не достаёт:
  // бедро на высоте 0.81, длина ноги 0.82, по горизонтали выходит около 0.41.
  stepReach: 0.20,
  // Насколько стопа уходит вбок от своего места при боковом ходе. Без
  // отдельного предела боец раскорячивался: замерено, при ходе вбок размах
  // опоры доходил до 108 см, по 54 от таза, и нога туда не достаёт.
  stepSide: 0.13,
  // Насколько близко к средней линии разрешено ставить стопу. Ниже этого
  // ноги начинают перекрещиваться.
  stanceCross: 0.05,
  // Пауза между шагами. Не даёт обеим ногам оторваться разом.
  stepGap: 0.02,
  // Во сколько раз мышца стопы крепче прочих. Множитель ОБЩЕЙ жёсткости,
  // и это важно: пока общая была 200, четвёрка здесь просто выравнивала
  // стопу с остальными. С жёсткостью 1400 та же четвёрка дала 5600, и
  // приколоченная к настилу тяжёлая стопа начала толкать тело через связи —
  // боец уезжал сам на 6 метров, стоя на месте, и разгонялся до 4.8 м/с
  // при заданных 1.6.
  footGrip: 2,
  // Во сколько раз стоящая стопа тяжелее для решателя. Меньше — крепче
  // упирается в настил. Единица означает «как всегда», и тогда собственная
  // нога утаскивает стопу через колено: замерено 42% проскальзывания.
  footAnchor: 0.06,
  stepLength: 0.28,
  stepBob: 0.07,
  stepLift: 0.13,

  // --- Вес и шаткость ---
  // Значения ниже прежних: тело теперь качается и заваливается само,
  // и вторая имитация поверх физики только спорила бы с ней.
  wobbleAmount: 0.22,
  wobbleRate: 1.1,
  leanFromAccel: 0.03,
  // Отставание дубины при развороте: отсюда у неё берётся вес.
  limbLag: 0.12,

  // --- Скрут корпуса ---
  // Плечевой пояс развёрнут относительно таза — то, чего у тела не было
  // вовсе. Замерено на прежней версии: за разворот на 180° плечи уходили
  // от таза на 3.6 градуса, при ходьбе на 5.9. Корпус проворачивался
  // цельным бруском, и тело от этого читалось деревянным.
  //
  // Отставание плеч от разворота, градусов на градус в секунду.
  twistFromTurn: 0.085,
  // Голова, наоборот, забегает вперёд: сначала смотрим, потом доводим корпус.
  headLead: 0.045,
  twistMax: 34,
  headTurnMax: 34,
  // Противофаза плеч и ног при ходьбе, градусов на полном ходу. Задание
  // намеренно выше, чем нужный результат: мягкие мышцы дотягивают плечи
  // примерно наполовину, замерено — 12.9° при цели 22.6.
  twistFromStep: 23,
  // Насколько корпус ведёт удар. Ноль — бьёт одна рука, и это заметно.
  twistFromSwing: 0.14,
  twistFromCharge: 12,
  // Дыхание в покое: единственное, что шевелит бойца, когда он стоит.
  breathRate: 0.32,
  breathTwist: 2.4,

  // --- Мышцы ---
  // Насколько крепко тело держит заданную позу. Отсюда растёт всё
  // остальное: баланс — это общая сила мышц, усталость — скорость её
  // восстановления, падение — сила, дошедшая до нуля.
  // Жёсткость — насколько быстро мышца дотягивает тело до цели.
  //
  // Высокая намеренно. Мягкие мышцы были хороши для цельного мягкого тела,
  // но у картонной куклы панель нарисована по кости, а кость стоит там, где
  // оказалась частица: не дотянула — панели разошлись и вошли друг в друга.
  // Замерено при 200: кисть отставала от цели на 11 см, локоть на 8.
  // При 700 это 2.0 и 1.7 см — сминание уходит.
  //
  // Выше не идём, и это найдено замером, а не осторожностью. При 1400
  // жёсткие ноги на приколоченной к настилу стопе начинают работать
  // рычагом, тело перекатывается через неё, и боец уезжает сам: 6 метров,
  // стоя на месте, и разгон до 4.8 м/с при заданных 1.6.
  muscleStiffness: 700,
  // Демпфер В ДОЛЯХ ОТ КРИТИЧЕСКОГО. Единица — приходит к цели без перелёта,
  // меньше — проскакивает и качается обратно. Почти единица здесь потому,
  // что перелёт у картонной куклы читается не желейностью, а сминанием:
  // панель уезжает за своё место и входит в соседнюю.
  muscleDamping: 0.9,
  // Какую часть собственного веса мышца несёт сама. Без неё жёсткость
  // и желейность были одной ручкой: слабая пружина уравновешивает вес ниже
  // цели, и боец вместе с мягкостью получал сутулость — при жёсткости 200
  // таз проседал с 0.85 до 0.74.
  muscleLift: 1,
  // Потолок ускорения мышцы. Единственное, что стоит между жёсткой пружиной
  // и взрывом решателя: сбитого уносит на метры, и без клампа мышца
  // ответила бы на такую ошибку рывком, которого Верле не переживёт.
  muscleMaxAccel: 260,
  // Насколько крепко кисть держит дубину. Ниже единицы оружие отстаёт
  // от замаха и не долетает; выше — идёт за рукой как приклеенное
  // и перестаёт весить.
  clubGrip: 1.2,
  // Ниже этой силы боец собой не управляет — он ещё поднимается.
  controlStrength: 0.55,
  // Скорость, ниже которой тело считается успокоившимся.
  settleSpeed: 0.6,
  // Сколько боец держит позу без опоры под ногами. Дальше мышцы отпускают:
  // сорвавшийся с кромки должен падать телом, а не стоять по стойке смирно.
  airReleaseTime: 0.35,

  // --- Удар ---
  // reach и drop теперь задают положение ХВАТА, а не центра дубины: кисти
  // обязаны оказаться там, куда руки дотягиваются. Прежние значения уводили
  // хват на 1.13 м от плеча при длине руки 0.68 — рука до дубины физически
  // не доставала и висела в воздухе отдельным обрубком.
  // Дубина в покое висит СБОКУ, а не за спиной по центру. Центр был
  // единственным местом, куда дотягивались обе руки, и держать её там
  // приходилось в обнимку перед собой. Теперь боец несёт её одной рукой,
  // как человек носит тяжёлое, а вторая приходит на рукоять на замахе.
  carryAngle: 100,
  carryReach: 0.30,
  carryDrop: -0.20,
  // Наклон дубины к земле в покое, градусов. Почти отвесно — она свисает
  // из опущенной руки и идёт набалдашником у самого настила.
  carryPitch: 72,
  chargeMoveSlow: 0.45,
  swingChargeTime: 0.5,
  // Замерено: тяжёлой дубине нужно время догнать дугу. При 0.18 с
  // набалдашник не доходил до цели метра на метр — оружие проносилось
  // мимо, ещё не успев вылететь. Длинный мах у тяжёлого оружия и должен
  // быть длинным, это же и есть цена вложения в удар.
  swingStrikeTime: 0.34,
  swingRecoverTime: 0.16,
  swingCooldown: 0.12,
  swingArcDegrees: 150,
  windUpReach: 0.50,
  handMaxReach: 0.62,
  swingWeakestPower: 0.5,

  // --- Удары ---
  // Пересчитано после перехода тела на мышцы. Раньше дугу вела скриптовая
  // формула и набалдашник разгонялся до 40 м/с; теперь дубина — настоящая
  // масса на кисти, и её физический предел около 15 м/с. Это и есть та самая
  // тяжесть оружия, которую требует замысел, но старые пороги на этом
  // диапазоне не срабатывали вовсе: удары просто перестали засчитываться.
  minImpactSpeed: 4,
  maxImpactSpeed: 17,
  // Замерено на стенде: незаряженный удар отбрасывает метра на четыре,
  // полный — метров на пять. С прежними числами любой удар в центре арены
  // выносил сразу за кромку, и край переставал быть местом, куда соперника
  // ещё надо подвести.
  minKnockback: 2.5,
  maxKnockback: 7,
  knockUpBias: 0.35,
  hitCooldown: 0.22,
  hitStopMax: 0.055,
  shakeMul: 0.9,

  // --- Ragdoll ---
  // Доля скорости, теряемая за секунду. 0 — идеальный вакуум, тело кувыркается вечно.
  ragdollDrag: 0.25,
  // Трение о настил, тоже за секунду. Низкое — тряпку несёт по арене и она
  // вылетает за край; высокое — прилипает там, где упала.
  ragdollFriction: 0.99,
  // Упругость отскока от настила.
  ragdollBounce: 0.12,
  // Сколько проходов решателя ограничений за шаг. Больше — жёстче скелет.
  ragdollIterations: 6,
  // Доля ошибки связи, снимаемая за один проход. Замерено: пока мышцы
  // держат позу, эта ручка почти ничего не решает — связи попросту не
  // нагружены, прогиб корпуса меняется на полсантиметра между 1.0 и 0.35.
  // Её место — падение, когда мышцы отпущены и скелет остаётся один
  // против настила и инерции.
  linkStiffness: 0.8,

  // --- Подъём после падения ---
  standUpSettle: 0.45,
  standUpTimeout: 3,
  standUpTime: 0.5,

  // --- Прицел ---
  aimMaxRadiusFactor: 1.35,
  showAimLink: true,

  // --- Камера ---
  camPitch: 55,
  camYaw: 0,
  camDistance: 21,
  // Запас вокруг арены при автоподгонке кадра. 1 — диск впритык по краям,
  // больше — с полями. camDistance при этом остаётся нижней границей.
  camFitMargin: 1.06,
  camFov: 42,
  camFollowWeight: 0.45,
  camSmooth: 0.18,
  camLookAhead: 0.35,

  // --- Песочница ---
  dummyCount: 3,
  dummyRespawnDelay: 1.5,
  showClubTrail: true,
  // Замедление по клавише F: видно форму дуги и момент касания.
  slowMotion: 0.25,
};

// Значения по умолчанию — для кнопки «сброс». Копия снимается до того,
// как localStorage успеет что-то перезаписать.
export const defaults = { ...tuning };

// Описание панели. Порядок и заголовки повторяют [Header] из GameTuning.
export const tuneGroups = [
  {
    title: 'Движение',
    items: [
      ['maxRunSpeed', 1, 9, 0.1],
      ['strafeSpeed', 0.3, 1, 0.02],
      ['backSpeed', 0.3, 1, 0.02],
      ['moveAccel', 2, 60, 0.5],
      ['moveBrake', 2, 60, 0.5],
      ['turnSpeed', 90, 1080, 10],
      ['turnAccel', 200, 6000, 50],
      ['swingMoveLock', 0, 1, 0.05],
      ['chargeMoveSlow', 0.1, 1, 0.05],
    ],
  },
  {
    title: 'Тело',
    items: [
      ['bodyMode', 2, 3, 1],
      ['headLevel', 0, 1, 0.05],
      ['torsoClearance', 0, 0.4, 0.01],
      ['partGap', 0, 0.12, 0.005],
    ],
  },
  {
    title: 'Стойка',
    items: [
      ['guardHeight', 0.7, 1.5, 0.01],
      ['guardForward', -0.1, 0.6, 0.01],
      ['guardWidth', 0.1, 0.6, 0.01],
      ['armSwing', 0, 1.5, 0.05],
      ['stanceWidth', 0, 0.3, 0.005],
      ['stanceCrouch', 0, 0.3, 0.005],
    ],
  },
  {
    title: 'Шаг',
    items: [
      ['stepTrigger', 0.08, 0.42, 0.01],
      ['stepTime', 0.08, 0.6, 0.01],
      ['stepLead', 0, 2, 0.05],
      ['stepReach', 0.05, 0.42, 0.01],
      ['stepSide', 0.05, 0.4, 0.01],
      ['stanceCross', 0, 0.2, 0.01],
      ['footGrip', 0.2, 8, 0.1],
      ['footAnchor', 0.02, 1, 0.02],
      ['stepGap', 0, 0.3, 0.01],
      ['stepLength', 0, 0.8, 0.01],
      ['stepBob', 0, 0.3, 0.005],
      ['stepLift', 0, 0.4, 0.005],
    ],
  },
  {
    title: 'Вес и шаткость',
    items: [
      ['wobbleAmount', 0, 2, 0.05],
      ['wobbleRate', 0.1, 4, 0.05],
      ['leanFromAccel', 0, 0.3, 0.005],
      ['limbLag', 0, 0.5, 0.01],
    ],
  },
  {
    title: 'Скрут корпуса',
    items: [
      ['twistFromTurn', 0, 0.2, 0.005],
      ['headLead', 0, 0.2, 0.005],
      ['twistMax', 0, 60, 1],
      ['headTurnMax', 0, 80, 1],
      ['twistFromStep', 0, 40, 0.5],
      ['twistFromSwing', 0, 0.5, 0.01],
      ['twistFromCharge', 0, 40, 0.5],
      ['breathRate', 0.05, 1.5, 0.01],
      ['breathTwist', 0, 12, 0.1],
    ],
  },
  {
    title: 'Мышцы',
    items: [
      ['muscleStiffness', 60, 1600, 10],
      ['muscleDamping', 0.1, 1.4, 0.02],
      ['muscleLift', 0, 1.2, 0.05],
      ['muscleMaxAccel', 30, 400, 5],
      ['clubGrip', 0.2, 5, 0.1],
      ['controlStrength', 0.1, 1, 0.05],
    ],
  },
  {
    title: 'Замах',
    items: [
      ['carryAngle', 90, 180, 1],
      ['carryReach', 0, 0.6, 0.02],
      ['carryDrop', -0.7, 0.2, 0.02],
      ['carryPitch', -30, 90, 2],
      ['swingChargeTime', 0.1, 1.5, 0.02],
      ['swingStrikeTime', 0.06, 0.5, 0.01],
      ['swingRecoverTime', 0.05, 0.6, 0.01],
      ['swingArcDegrees', 60, 260, 5],
      ['windUpReach', 0.1, 0.8, 0.02],
      ['handMaxReach', 0.3, 0.9, 0.02],
    ],
  },
  {
    title: 'Сила ударов',
    items: [
      ['minImpactSpeed', 0.5, 30, 0.5],
      ['maxImpactSpeed', 10, 70, 1],
      ['minKnockback', 0, 20, 0.5],
      ['maxKnockback', 3, 45, 0.5],
      ['knockUpBias', 0, 1.2, 0.05],
    ],
  },
  {
    title: 'Тряпка',
    items: [
      ['ragdollDrag', 0, 0.95, 0.05],
      ['ragdollFriction', 0.5, 0.999, 0.005],
      ['ragdollBounce', 0, 0.6, 0.02],
      ['linkStiffness', 0.1, 1, 0.05],
      ['ragdollIterations', 1, 10, 1],
      ['standUpSettle', 0, 2, 0.05],
      ['standUpTime', 0.1, 2, 0.05],
    ],
  },
  {
    title: 'Мир и камера',
    items: [
      ['gravity', -60, -6, 1],
      ['arenaRadius', 3, 16, 0.25],
      ['camPitch', 25, 85, 1],
      ['camDistance', 10, 40, 0.5],
      ['camFitMargin', 0.8, 1.6, 0.02],
      ['camFov', 20, 80, 1],
      ['camFollowWeight', 0, 1, 0.05],
      ['dummyCount', 0, 6, 1],
    ],
  },
  {
    title: 'Показывать',
    items: [
      ['withClub', 'bool'],
      ['showClubTrail', 'bool'],
      ['showAimLink', 'bool'],
    ],
  },
];

// Версия в ключе — не формальность. Появились стойка, скрут корпуса,
// мягкость связей и выключатель оружия, а мышцы пересчитаны под желейное
// тело. Сохранённые с прошлой версии числа перетёрли бы новые умолчания
// молча. Меняем ключ вместе со смыслом полей.
const STORAGE_KEY = 'topkong.tuning.v4';

/** Подтянуть сохранённые значения. Настройки переживают перезагрузку —
 *  иначе подбирать ощущения с телефона невозможно. */
export function loadTuning() {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) return;
    const saved = JSON.parse(raw);
    for (const key of Object.keys(defaults)) {
      if (key in saved && typeof saved[key] === typeof defaults[key]) {
        tuning[key] = saved[key];
      }
    }
  } catch (e) {
    // Приватный режим и заблокированное хранилище — не повод не запускать игру.
  }
}

export function saveTuning() {
  try {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(tuning));
  } catch (e) { /* см. выше */ }
}

export function resetTuning() {
  Object.assign(tuning, defaults);
  saveTuning();
}
